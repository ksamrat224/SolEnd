/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertIsInstructionWithAccounts,
  containsBytes,
  fixEncoderSize,
  getBytesEncoder,
  type Address,
  type Instruction,
  type InstructionWithData,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  parseCreateLoanInstruction,
  parseDepositPoolInstruction,
  parseInitializePoolInstruction,
  parseLiquidateLoanInstruction,
  parseRepayLoanInstruction,
  type ParsedCreateLoanInstruction,
  type ParsedDepositPoolInstruction,
  type ParsedInitializePoolInstruction,
  type ParsedLiquidateLoanInstruction,
  type ParsedRepayLoanInstruction,
} from "../instructions";

export const VAULT_PROGRAM_ADDRESS =
  "2P9pEjiLeri1nQBaqcYDJ3crQwasZ2p5BdJYMpPPXTFv" as Address<"2P9pEjiLeri1nQBaqcYDJ3crQwasZ2p5BdJYMpPPXTFv">;

export enum VaultAccount {
  BorrowerProfile,
  CollateralVault,
  LoanAccount,
  PoolAccount,
}

export function identifyVaultAccount(
  account: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): VaultAccount {
  const data = "data" in account ? account.data : account;
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([170, 170, 163, 235, 224, 250, 36, 78]),
      ),
      0,
    )
  ) {
    return VaultAccount.BorrowerProfile;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([19, 189, 95, 155, 100, 9, 159, 145]),
      ),
      0,
    )
  ) {
    return VaultAccount.CollateralVault;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([223, 49, 62, 167, 247, 182, 239, 60]),
      ),
      0,
    )
  ) {
    return VaultAccount.LoanAccount;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([116, 210, 187, 119, 196, 196, 52, 137]),
      ),
      0,
    )
  ) {
    return VaultAccount.PoolAccount;
  }
  throw new Error(
    "The provided account could not be identified as a vault account.",
  );
}

export enum VaultInstruction {
  CreateLoan,
  DepositPool,
  InitializePool,
  LiquidateLoan,
  RepayLoan,
}

export function identifyVaultInstruction(
  instruction: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): VaultInstruction {
  const data = "data" in instruction ? instruction.data : instruction;
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([166, 131, 118, 219, 138, 218, 206, 140]),
      ),
      0,
    )
  ) {
    return VaultInstruction.CreateLoan;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([187, 244, 122, 76, 155, 46, 239, 194]),
      ),
      0,
    )
  ) {
    return VaultInstruction.DepositPool;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([95, 180, 10, 172, 84, 174, 232, 40]),
      ),
      0,
    )
  ) {
    return VaultInstruction.InitializePool;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([111, 249, 185, 54, 161, 147, 178, 24]),
      ),
      0,
    )
  ) {
    return VaultInstruction.LiquidateLoan;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([224, 93, 144, 77, 61, 17, 137, 54]),
      ),
      0,
    )
  ) {
    return VaultInstruction.RepayLoan;
  }
  throw new Error(
    "The provided instruction could not be identified as a vault instruction.",
  );
}

export type ParsedVaultInstruction<
  TProgram extends string = "2P9pEjiLeri1nQBaqcYDJ3crQwasZ2p5BdJYMpPPXTFv",
> =
  | ({
      instructionType: VaultInstruction.CreateLoan;
    } & ParsedCreateLoanInstruction<TProgram>)
  | ({
      instructionType: VaultInstruction.DepositPool;
    } & ParsedDepositPoolInstruction<TProgram>)
  | ({
      instructionType: VaultInstruction.InitializePool;
    } & ParsedInitializePoolInstruction<TProgram>)
  | ({
      instructionType: VaultInstruction.LiquidateLoan;
    } & ParsedLiquidateLoanInstruction<TProgram>)
  | ({
      instructionType: VaultInstruction.RepayLoan;
    } & ParsedRepayLoanInstruction<TProgram>);

export function parseVaultInstruction<TProgram extends string>(
  instruction: Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array>,
): ParsedVaultInstruction<TProgram> {
  const instructionType = identifyVaultInstruction(instruction);
  switch (instructionType) {
    case VaultInstruction.CreateLoan: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: VaultInstruction.CreateLoan,
        ...parseCreateLoanInstruction(instruction),
      };
    }
    case VaultInstruction.DepositPool: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: VaultInstruction.DepositPool,
        ...parseDepositPoolInstruction(instruction),
      };
    }
    case VaultInstruction.InitializePool: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: VaultInstruction.InitializePool,
        ...parseInitializePoolInstruction(instruction),
      };
    }
    case VaultInstruction.LiquidateLoan: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: VaultInstruction.LiquidateLoan,
        ...parseLiquidateLoanInstruction(instruction),
      };
    }
    case VaultInstruction.RepayLoan: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: VaultInstruction.RepayLoan,
        ...parseRepayLoanInstruction(instruction),
      };
    }
    default:
      throw new Error(
        `Unrecognized instruction type: ${instructionType as string}`,
      );
  }
}
