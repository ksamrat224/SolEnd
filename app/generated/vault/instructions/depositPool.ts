/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findPoolPda } from "../pdas";
import { VAULT_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const DEPOSIT_POOL_DISCRIMINATOR = new Uint8Array([
  187, 244, 122, 76, 155, 46, 239, 194,
]);

export function getDepositPoolDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    DEPOSIT_POOL_DISCRIMINATOR,
  );
}

export type DepositPoolInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountLender extends string | AccountMeta<string> = string,
  TAccountPool extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountLender extends string
        ? WritableSignerAccount<TAccountLender> &
            AccountSignerMeta<TAccountLender>
        : TAccountLender,
      TAccountPool extends string
        ? WritableAccount<TAccountPool>
        : TAccountPool,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type DepositPoolInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
};

export type DepositPoolInstructionDataArgs = { amount: number | bigint };

export function getDepositPoolInstructionDataEncoder(): FixedSizeEncoder<DepositPoolInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amount", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: DEPOSIT_POOL_DISCRIMINATOR }),
  );
}

export function getDepositPoolInstructionDataDecoder(): FixedSizeDecoder<DepositPoolInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amount", getU64Decoder()],
  ]);
}

export function getDepositPoolInstructionDataCodec(): FixedSizeCodec<
  DepositPoolInstructionDataArgs,
  DepositPoolInstructionData
> {
  return combineCodec(
    getDepositPoolInstructionDataEncoder(),
    getDepositPoolInstructionDataDecoder(),
  );
}

export type DepositPoolAsyncInput<
  TAccountLender extends string = string,
  TAccountPool extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  lender: TransactionSigner<TAccountLender>;
  pool?: Address<TAccountPool>;
  systemProgram?: Address<TAccountSystemProgram>;
  amount: DepositPoolInstructionDataArgs["amount"];
};

export async function getDepositPoolInstructionAsync<
  TAccountLender extends string,
  TAccountPool extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: DepositPoolAsyncInput<
    TAccountLender,
    TAccountPool,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  DepositPoolInstruction<
    TProgramAddress,
    TAccountLender,
    TAccountPool,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    lender: { value: input.lender ?? null, isWritable: true },
    pool: { value: input.pool ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.pool.value) {
    accounts.pool.value = await findPoolPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.lender),
      getAccountMeta(accounts.pool),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getDepositPoolInstructionDataEncoder().encode(
      args as DepositPoolInstructionDataArgs,
    ),
    programAddress,
  } as DepositPoolInstruction<
    TProgramAddress,
    TAccountLender,
    TAccountPool,
    TAccountSystemProgram
  >);
}

export type DepositPoolInput<
  TAccountLender extends string = string,
  TAccountPool extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  lender: TransactionSigner<TAccountLender>;
  pool: Address<TAccountPool>;
  systemProgram?: Address<TAccountSystemProgram>;
  amount: DepositPoolInstructionDataArgs["amount"];
};

export function getDepositPoolInstruction<
  TAccountLender extends string,
  TAccountPool extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: DepositPoolInput<TAccountLender, TAccountPool, TAccountSystemProgram>,
  config?: { programAddress?: TProgramAddress },
): DepositPoolInstruction<
  TProgramAddress,
  TAccountLender,
  TAccountPool,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    lender: { value: input.lender ?? null, isWritable: true },
    pool: { value: input.pool ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.lender),
      getAccountMeta(accounts.pool),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getDepositPoolInstructionDataEncoder().encode(
      args as DepositPoolInstructionDataArgs,
    ),
    programAddress,
  } as DepositPoolInstruction<
    TProgramAddress,
    TAccountLender,
    TAccountPool,
    TAccountSystemProgram
  >);
}

export type ParsedDepositPoolInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    lender: TAccountMetas[0];
    pool: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: DepositPoolInstructionData;
};

export function parseDepositPoolInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedDepositPoolInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      lender: getNextAccount(),
      pool: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getDepositPoolInstructionDataDecoder().decode(instruction.data),
  };
}
