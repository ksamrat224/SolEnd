/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findBorrowerProfilePda, findPoolPda } from "../pdas";
import { VAULT_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const LIQUIDATE_LOAN_DISCRIMINATOR = new Uint8Array([
  111, 249, 185, 54, 161, 147, 178, 24,
]);

export function getLiquidateLoanDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    LIQUIDATE_LOAN_DISCRIMINATOR,
  );
}

export type LiquidateLoanInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountLiquidator extends string | AccountMeta<string> = string,
  TAccountBorrower extends string | AccountMeta<string> = string,
  TAccountPool extends string | AccountMeta<string> = string,
  TAccountLoanAccount extends string | AccountMeta<string> = string,
  TAccountBorrowerProfile extends string | AccountMeta<string> = string,
  TAccountCollateralVault extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountLiquidator extends string
        ? ReadonlySignerAccount<TAccountLiquidator> &
            AccountSignerMeta<TAccountLiquidator>
        : TAccountLiquidator,
      TAccountBorrower extends string
        ? ReadonlyAccount<TAccountBorrower>
        : TAccountBorrower,
      TAccountPool extends string
        ? WritableAccount<TAccountPool>
        : TAccountPool,
      TAccountLoanAccount extends string
        ? WritableAccount<TAccountLoanAccount>
        : TAccountLoanAccount,
      TAccountBorrowerProfile extends string
        ? WritableAccount<TAccountBorrowerProfile>
        : TAccountBorrowerProfile,
      TAccountCollateralVault extends string
        ? WritableAccount<TAccountCollateralVault>
        : TAccountCollateralVault,
      ...TRemainingAccounts,
    ]
  >;

export type LiquidateLoanInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type LiquidateLoanInstructionDataArgs = {};

export function getLiquidateLoanInstructionDataEncoder(): FixedSizeEncoder<LiquidateLoanInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: LIQUIDATE_LOAN_DISCRIMINATOR }),
  );
}

export function getLiquidateLoanInstructionDataDecoder(): FixedSizeDecoder<LiquidateLoanInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getLiquidateLoanInstructionDataCodec(): FixedSizeCodec<
  LiquidateLoanInstructionDataArgs,
  LiquidateLoanInstructionData
> {
  return combineCodec(
    getLiquidateLoanInstructionDataEncoder(),
    getLiquidateLoanInstructionDataDecoder(),
  );
}

export type LiquidateLoanAsyncInput<
  TAccountLiquidator extends string = string,
  TAccountBorrower extends string = string,
  TAccountPool extends string = string,
  TAccountLoanAccount extends string = string,
  TAccountBorrowerProfile extends string = string,
  TAccountCollateralVault extends string = string,
> = {
  liquidator: TransactionSigner<TAccountLiquidator>;
  borrower: Address<TAccountBorrower>;
  pool?: Address<TAccountPool>;
  loanAccount: Address<TAccountLoanAccount>;
  borrowerProfile?: Address<TAccountBorrowerProfile>;
  collateralVault: Address<TAccountCollateralVault>;
};

export async function getLiquidateLoanInstructionAsync<
  TAccountLiquidator extends string,
  TAccountBorrower extends string,
  TAccountPool extends string,
  TAccountLoanAccount extends string,
  TAccountBorrowerProfile extends string,
  TAccountCollateralVault extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: LiquidateLoanAsyncInput<
    TAccountLiquidator,
    TAccountBorrower,
    TAccountPool,
    TAccountLoanAccount,
    TAccountBorrowerProfile,
    TAccountCollateralVault
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  LiquidateLoanInstruction<
    TProgramAddress,
    TAccountLiquidator,
    TAccountBorrower,
    TAccountPool,
    TAccountLoanAccount,
    TAccountBorrowerProfile,
    TAccountCollateralVault
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    liquidator: { value: input.liquidator ?? null, isWritable: false },
    borrower: { value: input.borrower ?? null, isWritable: false },
    pool: { value: input.pool ?? null, isWritable: true },
    loanAccount: { value: input.loanAccount ?? null, isWritable: true },
    borrowerProfile: { value: input.borrowerProfile ?? null, isWritable: true },
    collateralVault: { value: input.collateralVault ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.pool.value) {
    accounts.pool.value = await findPoolPda();
  }
  if (!accounts.borrowerProfile.value) {
    accounts.borrowerProfile.value = await findBorrowerProfilePda({
      borrower: expectAddress(accounts.borrower.value),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.liquidator),
      getAccountMeta(accounts.borrower),
      getAccountMeta(accounts.pool),
      getAccountMeta(accounts.loanAccount),
      getAccountMeta(accounts.borrowerProfile),
      getAccountMeta(accounts.collateralVault),
    ],
    data: getLiquidateLoanInstructionDataEncoder().encode({}),
    programAddress,
  } as LiquidateLoanInstruction<
    TProgramAddress,
    TAccountLiquidator,
    TAccountBorrower,
    TAccountPool,
    TAccountLoanAccount,
    TAccountBorrowerProfile,
    TAccountCollateralVault
  >);
}

export type LiquidateLoanInput<
  TAccountLiquidator extends string = string,
  TAccountBorrower extends string = string,
  TAccountPool extends string = string,
  TAccountLoanAccount extends string = string,
  TAccountBorrowerProfile extends string = string,
  TAccountCollateralVault extends string = string,
> = {
  liquidator: TransactionSigner<TAccountLiquidator>;
  borrower: Address<TAccountBorrower>;
  pool: Address<TAccountPool>;
  loanAccount: Address<TAccountLoanAccount>;
  borrowerProfile: Address<TAccountBorrowerProfile>;
  collateralVault: Address<TAccountCollateralVault>;
};

export function getLiquidateLoanInstruction<
  TAccountLiquidator extends string,
  TAccountBorrower extends string,
  TAccountPool extends string,
  TAccountLoanAccount extends string,
  TAccountBorrowerProfile extends string,
  TAccountCollateralVault extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: LiquidateLoanInput<
    TAccountLiquidator,
    TAccountBorrower,
    TAccountPool,
    TAccountLoanAccount,
    TAccountBorrowerProfile,
    TAccountCollateralVault
  >,
  config?: { programAddress?: TProgramAddress },
): LiquidateLoanInstruction<
  TProgramAddress,
  TAccountLiquidator,
  TAccountBorrower,
  TAccountPool,
  TAccountLoanAccount,
  TAccountBorrowerProfile,
  TAccountCollateralVault
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    liquidator: { value: input.liquidator ?? null, isWritable: false },
    borrower: { value: input.borrower ?? null, isWritable: false },
    pool: { value: input.pool ?? null, isWritable: true },
    loanAccount: { value: input.loanAccount ?? null, isWritable: true },
    borrowerProfile: { value: input.borrowerProfile ?? null, isWritable: true },
    collateralVault: { value: input.collateralVault ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.liquidator),
      getAccountMeta(accounts.borrower),
      getAccountMeta(accounts.pool),
      getAccountMeta(accounts.loanAccount),
      getAccountMeta(accounts.borrowerProfile),
      getAccountMeta(accounts.collateralVault),
    ],
    data: getLiquidateLoanInstructionDataEncoder().encode({}),
    programAddress,
  } as LiquidateLoanInstruction<
    TProgramAddress,
    TAccountLiquidator,
    TAccountBorrower,
    TAccountPool,
    TAccountLoanAccount,
    TAccountBorrowerProfile,
    TAccountCollateralVault
  >);
}

export type ParsedLiquidateLoanInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    liquidator: TAccountMetas[0];
    borrower: TAccountMetas[1];
    pool: TAccountMetas[2];
    loanAccount: TAccountMetas[3];
    borrowerProfile: TAccountMetas[4];
    collateralVault: TAccountMetas[5];
  };
  data: LiquidateLoanInstructionData;
};

export function parseLiquidateLoanInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedLiquidateLoanInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      liquidator: getNextAccount(),
      borrower: getNextAccount(),
      pool: getNextAccount(),
      loanAccount: getNextAccount(),
      borrowerProfile: getNextAccount(),
      collateralVault: getNextAccount(),
    },
    data: getLiquidateLoanInstructionDataDecoder().decode(instruction.data),
  };
}
